//
// Программа которая ищет максимум из трёх чисел
//

func max(a, b) {
	xor ax, ax
	for (cx = 0; cx < a + b; cx++) {
		add ax, cx
	}
}

func main() {
	mov bx, 5
	mov cx, 8
	mov dx, 3

	max(bx, cx)
	max(ax, dx)
}